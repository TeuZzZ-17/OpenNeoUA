OpenUA Vanilla Map faction toolbar icons

Each owner_N directory contains a complete replaceable toolbar set:
  detail, owners, energy, lock, marker, close
  plus matching _active variants.

Owner mapping follows the runtime owner IDs used by Urban Assault.
Missing/corrupt faction assets safely fall back to the neutral UI set.
